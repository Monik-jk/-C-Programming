#include <stdio.h>

int main()
{
  char ch;
  printf ("Enter a alphabet");
  scanf (%c, &ch);
switch(ch)
  case 1 "a" :
  case 2 "e" :
  case 3 "i" :
  case 4 "o" :
  case 5 "u" :
      printf( "%c is a vowal",&ch);
default:
      printf("%c is a consonent");
}
